# Bear Phone POS - نظام مبيعات دب فون

نظام نقاط البيع (POS) المتكامل لمتجر دب فون - متخصص في بيع الهواتف المحمولة والإكسسوارات.

## 🌐 Live Demo
**Frontend**: https://q7emar35hnfp2.ok.kimi.link

## ✅ المميزات الرئيسية

### 1. واجهة عربية كاملة مع دعم RTL
- خط Cairo للنصوص العربية
- اتجاه RTL كامل للواجهة
- تصميم متجاوب يعمل على جميع الأجهزة

### 2. إنشاء فواتير احترافية بصيغة PDF
- تصميم مطابق للفاتورة المرفوعة
- شعار دب فون في الرأس
- جدول المنتجات: [الاجمالي] | [السعر] | [العدد] | [الصنف]
- قسم الإجماليات: الاجمالي | الخصم | الصافي | المدفوع | المتبقي
- قسم الملاحظات في الأسفل

### 3. إرسال الفواتير عبر واتساب تلقائياً
- تكامل مع UltraMsg API
- رسالة عربية تحتوي على رقم الفاتورة والصافي والملاحظات
- رابط مباشر للفاتورة PDF

### 4. إدارة الضمانات
- خيارات متعددة: بدون ضمان، أسبوع، شهر، 3 أشهر، 6 أشهر، سنة، سنتين
- حساب تاريخ انتهاء الضمان تلقائياً
- متابعة الضمانات السارية

### 5. لوحة تحكم إحصائية
- إجمالي المبيعات
- إجمالي الإيرادات
- الضمانات السارية
- مبيعات اليوم

## 🛠️ التقنيات المستخدمة

- **Frontend**: React 19 + TypeScript + Vite + Tailwind CSS + shadcn/ui
- **Backend**: Express.js + TypeScript
- **Database**: SQLite (better-sqlite3) + Drizzle ORM
- **PDF Generation**: PDFKit
- **WhatsApp**: UltraMsg API + node-fetch

## 📁 هيكل المشروع

```
app/
├── server/                    # الخادم (Backend)
│   ├── src/
│   │   ├── routes/sales.ts    # نقاط نهاية API
│   │   ├── utils/whatsapp.ts  # تكامل الواتساب
│   │   ├── utils/pdf.ts       # توليد PDF
│   │   ├── types/index.ts     # أنواع TypeScript
│   │   └── index.ts           # نقطة دخول الخادم
│   └── database/
│       ├── schema.ts          # مخطط قاعدة البيانات
│       └── db.ts              # اتصال قاعدة البيانات
├── src/                       # الواجهة الأمامية (Frontend)
│   ├── sections/              # مكونات React
│   │   ├── Header.tsx
│   │   ├── Dashboard.tsx
│   │   ├── NewSaleForm.tsx
│   │   └── SalesList.tsx
│   ├── services/api.ts        # عميل API
│   ├── types/index.ts         # أنواع TypeScript
│   └── App.tsx                # التطبيق الرئيسي
└── dist/                      # الملفات المبنية
```

## 🚀 التشغيل المحلي

### 1. تثبيت الحزم

```bash
cd /mnt/okcomputer/output/app
npm install
```

### 2. إعداد متغيرات البيئة

```bash
cp .env.example .env
```

قم بتعديل ملف `.env`:

```env
# الخادم
PORT=3000

# واتساب UltraMsg
ULTRAMSG_INSTANCE_ID=your_instance_id
ULTRAMSG_TOKEN=your_token

# الرابط الأساسي
BASE_URL=http://localhost:3000
```

### 3. تشغيل الخادم

```bash
npm run server
```

### 4. تشغيل الواجهة الأمامية (Terminal آخر)

```bash
npm run dev
```

## 📋 نقاط النهاية (API Endpoints)

| المسار | الطريقة | الوصف |
|--------|---------|-------|
| `/api/sales` | POST | إنشاء فاتورة جديدة |
| `/api/sales` | GET | جلب جميع الفواتير |
| `/api/sales/:id` | GET | جلب فاتورة محددة |
| `/api/sales/:id/pdf` | GET | تحميل فاتورة PDF |
| `/api/sales/stats/dashboard` | GET | إحصائيات لوحة التحكم |

## 📄 تصميم الفاتورة PDF

تتكون الفاتورة من الأقسام التالية:

### الرأس
- شعار دب فون (دائرة زرقاء مع 🐻)
- BEAR PHONE / دب فون
- عنوان: "فاتورة بيع ضريبية مبسطة"

### البيانات
- التاريخ: YYYY-MM-DD HH:MM:SS AM/PM
- المكرم: [اسم العميل]
- رقم الفاتورة: [ID]

### جدول المنتجات
| الاجمالي | السعر | العدد | الصنف |
|----------|-------|-------|-------|

### الإجماليات
- الاجمالي
- الخصم
- الصافي (بالأخضر)
- المدفوع
- المتبقي على العميل

### الملاحظات
- يظهر في أسفل الفاتورة
- إذا كانت فارغة يظهر "لا يوجد"

## 💬 قالب رسالة الواتساب

```
مرحباً بك في *BEAR PHONE* 🐻

تم إصدار فاتورتك بنجاح:
🧾 رقم الفاتورة: {id}
💰 الصافي: {netAmount} ريال
📝 ملاحظات: {notes || 'لا يوجد'}

📍 رابط الفاتورة: {link}
```

## 🔧 الإصلاحات التقنية

1. **تاريخ الخادم**: يتم إنشاء التاريخ على الخادم مباشرة (`new Date()`) وليس من العميل
2. **الملاحظات**: حقل اختياري - يظهر "لا يوجد" إذا كان فارغاً
3. **الواتساب**: إذا فشل الإرسال، تُحفظ الفاتورة مع تسجيل الخطأ
4. **الضمان**: حساب تاريخ الانتهاء تلقائياً حسب الاختيار

## 📱 التصميم المتجاوب

- النموذج يتراص عمودياً على الهواتف
- الجدول يسمح بالتمرير الأفقي عند الحاجة
- جميع العناصر تتكيف مع حجم الشاشة

## 📝 الترخيص

جميع الحقوق محفوظة © 2024 Bear Phone - دب فون
